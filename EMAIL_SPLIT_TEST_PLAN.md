# 邮箱分割功能测试计划

## 测试概述

本测试计划针对邮箱分割功能的完整流程进行测试，确保所有功能按需求正常工作。

## 测试环境

- 操作系统：Windows/macOS/Linux
- Flutter版本：3.7.0+
- 设备：桌面应用

## 测试用例

### 1. 文件上传功能测试

#### 1.1 文件读取失败测试
- **测试目标**：验证文件读取失败时的错误提示
- **测试步骤**：
  1. 选择一个损坏的文件
  2. 点击开始分割
- **预期结果**：显示"文件读取失败"的提示信息

#### 1.2 TXT文件处理测试
- **测试目标**：验证TXT文件的正常处理
- **测试步骤**：
  1. 上传一个包含邮箱列表的TXT文件
  2. 验证文件类型识别为"txt"
  3. 验证不显示列选择界面
- **预期结果**：文件正常读取，不显示列选择

#### 1.3 CSV文件处理测试
- **测试目标**：验证CSV文件的正常处理
- **测试步骤**：
  1. 上传一个多列的CSV文件
  2. 验证文件类型识别为"csv"
  3. 验证显示列选择界面
  4. 选择不同的邮箱列
- **预期结果**：正确识别文件类型，显示列选择，能正确读取指定列的邮箱

#### 1.4 Excel文件处理测试
- **测试目标**：验证Excel文件的正常处理
- **测试步骤**：
  1. 上传一个Excel文件
  2. 验证文件类型识别为"excel"
  3. 验证显示列选择界面
- **预期结果**：正确识别文件类型，显示列选择

### 2. 排除邮箱功能测试

#### 2.1 分隔符选择测试
- **测试目标**：验证不同分隔符的正确处理
- **测试步骤**：
  1. 在排除邮箱输入框中输入不同分隔符分隔的邮箱
  2. 分别选择逗号、分号、空格、换行、制表符
  3. 验证分割结果正确
- **预期结果**：每种分隔符都能正确分割排除邮箱

#### 2.2 排除邮箱处理测试
- **测试目标**：验证排除邮箱功能正常工作
- **测试步骤**：
  1. 上传包含邮箱的文件
  2. 在排除邮箱中输入部分邮箱
  3. 执行分割
- **预期结果**：排除的邮箱不会出现在最终结果中

### 3. 处理设置功能测试

#### 3.1 跳过空邮箱测试
- **测试目标**：验证空邮箱过滤功能
- **测试步骤**：
  1. 上传包含空行的文件
  2. 开启"跳过空邮箱"选项
  3. 执行分割
- **预期结果**：空行被过滤掉

#### 3.2 忽略无效邮箱测试
- **测试目标**：验证无效邮箱过滤功能
- **测试步骤**：
  1. 上传包含无效邮箱格式的文件
  2. 开启"忽略无效邮箱"选项
  3. 执行分割
- **预期结果**：无效邮箱被过滤掉

#### 3.3 去除重复邮箱测试
- **测试目标**：验证去重功能
- **测试步骤**：
  1. 上传包含重复邮箱的文件
  2. 开启"去除重复邮箱"选项
  3. 执行分割
- **预期结果**：重复邮箱被去除

#### 3.4 去除空白字符测试
- **测试目标**：验证空白字符处理功能
- **测试步骤**：
  1. 上传包含前后空格的邮箱文件
  2. 开启"去除空白字符"选项
  3. 执行分割
- **预期结果**：邮箱前后的空白字符被去除

### 4. 分割设置功能测试

#### 4.1 固定大小分割测试
- **测试目标**：验证固定大小分割功能
- **测试步骤**：
  1. 选择"固定大小"模式
  2. 设置分割大小为1000
  3. 执行分割
- **预期结果**：邮箱按1000个一组进行分割

#### 4.2 自定义规则分割测试
- **测试目标**：验证自定义分割规则功能
- **测试步骤**：
  1. 选择"自定义规则"模式
  2. 设置多个分割规则
  3. 执行分割
- **预期结果**：按照自定义规则进行分割

### 5. 保存目录设置测试

#### 5.1 默认保存目录测试
- **测试目标**：验证默认保存目录功能
- **测试步骤**：
  1. 不选择保存目录
  2. 执行分割
- **预期结果**：在文件上传目录下创建data文件夹并保存文件

#### 5.2 自定义保存目录测试
- **测试目标**：验证自定义保存目录功能
- **测试步骤**：
  1. 选择自定义保存目录
  2. 执行分割
- **预期结果**：文件保存到选择的目录中

### 6. 进度条显示测试

#### 6.1 进度条显示测试
- **测试目标**：验证进度条正常显示
- **测试步骤**：
  1. 开始分割操作
  2. 观察进度条显示
- **预期结果**：进度条显示处理进度，包含百分比和状态文本

### 7. 文件保存测试

#### 7.1 文件命名测试
- **测试目标**：验证文件命名规则
- **测试步骤**：
  1. 上传名为"emails.csv"的文件
  2. 执行分割
- **预期结果**：生成的文件名为"emails_1.csv"、"emails_2.csv"等

#### 7.2 文件格式测试
- **测试目标**：验证保存的文件格式
- **测试步骤**：
  1. 执行分割
  2. 检查保存的CSV文件
- **预期结果**：文件包含标题行"email"和数据行

### 8. 错误处理测试

#### 8.1 文件读取错误测试
- **测试目标**：验证文件读取错误的处理
- **测试步骤**：
  1. 选择不存在的文件
  2. 尝试读取
- **预期结果**：显示适当的错误提示

#### 8.2 保存目录错误测试
- **测试目标**：验证保存目录错误的处理
- **测试步骤**：
  1. 选择无权限的目录
  2. 执行分割
- **预期结果**：显示适当的错误提示

## 测试数据准备

### 测试文件
1. **正常TXT文件**：包含邮箱列表的文本文件
2. **多列CSV文件**：包含邮箱、姓名、公司等列的CSV文件
3. **Excel文件**：包含邮箱数据的Excel文件
4. **损坏文件**：无法读取的文件
5. **包含无效邮箱的文件**：包含格式错误的邮箱
6. **包含重复邮箱的文件**：包含重复邮箱地址的文件

### 测试邮箱数据
- 有效邮箱：test@example.com, user@gmail.com
- 无效邮箱：invalid-email, @example.com, test@
- 重复邮箱：test@example.com（多次出现）

## 测试执行计划

### 第一轮测试
- 执行所有基础功能测试
- 重点测试文件上传和基本处理功能

### 第二轮测试
- 执行高级功能测试
- 重点测试分割规则和保存功能

### 第三轮测试
- 执行错误处理和边界条件测试
- 重点测试异常情况的处理

## 测试报告模板

### 测试结果记录
- 测试用例编号
- 测试结果（通过/失败）
- 问题描述（如果有）
- 截图或日志

### 问题跟踪
- 问题严重程度
- 问题描述
- 复现步骤
- 修复状态

## 验收标准

1. **功能完整性**：所有需求功能都能正常工作
2. **用户体验**：界面友好，操作流畅
3. **错误处理**：异常情况有适当的提示和处理
4. **性能表现**：处理大量数据时性能良好
5. **文件保存**：分割文件正确保存到指定位置

## 测试完成标准

- 所有测试用例执行完成
- 无严重bug
- 功能符合需求文档
- 用户体验良好